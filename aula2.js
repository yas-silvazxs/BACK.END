// const = variavl constante, o valor armazenado não pode ser alterado
// let = variavel global
// var = variavel global
const v1 = 1500
const v2 = 2
const soma = v1 + v2
const sub = v1 - v2 
const mul = v1 * v2
const div = v1 / v2

let resultado = v1 + v2 
console.log("a soma e " +  resultado)
resultado =  v1 - v2
console.log("a soma e " +  resultado)
resultado = v1 * v2

console.log("a soma é : + soma" )
console.log("a subtração é : - sub " )
console.log("a multiplicação é : * mul" )
console.log("a divisão é : / div" )